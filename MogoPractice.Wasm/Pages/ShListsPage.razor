@page "/shopping-lists"
@using MongoPractice.Contracts.V1.Views

<h3>My Shopping Lists</h3>

@if (ShListViews == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <div class="row">
        @* Explicit type used here instead of 'var' *@
        @foreach (ShListViewV1 shListView in ShListViews)
        {
            <div class="col-md-4 mb-3">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">@shListView.Name</h5>
                    </div>
                </div>
            </div>
        }
    </div>
}